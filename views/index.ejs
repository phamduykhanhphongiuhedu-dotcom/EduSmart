<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduLink AI - Nền tảng Giáo dục Chia sẻ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary: #4f46e5;
            --primary-dark: #4338ca;
            --secondary: #0ea5e9;
            --accent: #f43f5e;
            --dark: #0f172a;
            --light: #f8fafc;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--dark);
            background-color: #fff;
            overflow-x: hidden;
        }

        /* 1. NAVBAR SIÊU CẤP */
        .navbar {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(20px);
            padding: 15px 0;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.03);
        }
        .navbar-brand {
            font-weight: 800;
            font-size: 1.6rem;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .nav-link { font-weight: 600; color: #475569; margin: 0 10px; transition: 0.3s; }
        .nav-link:hover { color: var(--primary); }
        .btn-nav { padding: 10px 25px; border-radius: 50px; font-weight: 700; transition: 0.3s; }
        .btn-login { background: #f1f5f9; color: var(--dark); border: none; }
        .btn-login:hover { background: #e2e8f0; }
        .btn-register { background: var(--primary); color: white; border: none; box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3); }
        .btn-register:hover { background: var(--primary-dark); transform: translateY(-2px); }

        /* 2. HERO SECTION */
        .hero-section {
            padding: 120px 0 80px 0;
            background: radial-gradient(circle at top right, #e0e7ff, transparent 40%),
                        radial-gradient(circle at bottom left, #e0f2fe, transparent 40%);
            text-align: center;
        }
        .hero-badge {
            background: rgba(79, 70, 229, 0.1);
            color: var(--primary);
            padding: 8px 20px;
            border-radius: 30px;
            font-weight: 700;
            font-size: 0.9rem;
            display: inline-block;
            margin-bottom: 20px;
        }
        .hero-title { font-size: 3.5rem; font-weight: 800; margin-bottom: 20px; line-height: 1.2; }
        .hero-subtitle { font-size: 1.2rem; color: #64748b; margin-bottom: 40px; max-width: 600px; margin-left: auto; margin-right: auto; }

        /* SEARCH WRAPPER */
        .search-wrapper {
            background: white; padding: 10px; border-radius: 100px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1); max-width: 800px;
            margin: 0 auto; display: flex; align-items: center;
            border: 1px solid rgba(0,0,0,0.05); transition: 0.3s;
        }
        .search-wrapper:focus-within {
            transform: scale(1.02); border-color: var(--primary);
            box-shadow: 0 30px 60px rgba(79, 70, 229, 0.15);
        }
        .search-icon { font-size: 1.2rem; color: #94a3b8; margin-left: 20px; }
        .search-input { border: none; outline: none; flex: 1; padding: 15px 20px; font-size: 1.1rem; }
        .btn-search {
            background: var(--primary); color: white; border: none;
            width: 55px; height: 55px; border-radius: 50%;
            font-size: 1.2rem; cursor: pointer; transition: 0.3s;
        }
        .btn-search:hover { background: var(--primary-dark); rotate: 90deg; }

        /* 3. VAI TRÒ */
        .role-switch-container { margin-top: 60px; display: flex; justify-content: center; gap: 20px; }
        .role-card {
            background: white; padding: 20px 30px; border-radius: 20px;
            border: 2px solid #f1f5f9; cursor: pointer; transition: 0.3s;
            display: flex; align-items: center; gap: 15px; text-decoration: none; color: var(--dark);
        }
        .role-card:hover { border-color: var(--primary); transform: translateY(-5px); box-shadow: 0 10px 30px rgba(0,0,0,0.05); }
        .role-icon-box { width: 50px; height: 50px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.5rem; }
        .bg-student { background: #e0e7ff; color: var(--primary); }
        .bg-teacher { background: #ffedd5; color: #f97316; }

        /* 4. GRID KHÓA HỌC */
        .section-header { margin-bottom: 50px; text-align: center; }
        .course-card {
            background: white; border-radius: 20px; overflow: hidden;
            border: 1px solid #f1f5f9; transition: 0.3s; height: 100%; position: relative;
        }
        .course-card:hover { transform: translateY(-10px); box-shadow: 0 20px 40px rgba(0,0,0,0.08); }
        
        /* Cập nhật mới: Phần ảnh bìa khóa học */
        .course-thumb-img {
            width: 100%; height: 200px; object-fit: cover;
            border-bottom: 1px solid #f1f5f9;
        }
        .course-body { padding: 25px; }
        .badge-token {
            background: var(--dark); color: #fbbf24; padding: 5px 12px;
            border-radius: 30px; font-size: 0.8rem; font-weight: 700; display: inline-flex; align-items: center; gap: 5px;
        }

        /* 5. STATS & FOOTER */
        .stat-section { background: var(--dark); color: white; padding: 80px 0; margin-top: 80px; border-radius: 30px; margin-bottom: 50px; }
        .stat-item h2 { font-size: 3rem; font-weight: 800; color: var(--secondary); margin-bottom: 10px; }
        .footer { padding: 50px 0 20px; border-top: 1px solid #f1f5f9; background: white; }
        
        /* CHATBOT */
        .chatbot-widget {
            position: fixed; bottom: 30px; right: 30px; width: 360px;
            background: white; border-radius: 24px; box-shadow: 0 20px 60px rgba(0,0,0,0.2);
            z-index: 9999; border: 1px solid #e2e8f0; overflow: hidden;
        }
        .chat-header { background: var(--primary); color: white; padding: 15px 20px; font-weight: 700; display: flex; justify-content: space-between; align-items: center;}
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#"><i class="fas fa-cube"></i> EduLink AI</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav align-items-center gap-3">
                    <li class="nav-item"><a class="nav-link" href="#">Về chúng tôi</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Khóa học</a></li>
                    <% if (user) { %>
                        <li class="nav-item"><a href="/learner" class="btn btn-nav btn-primary text-white">Vào lớp học</a></li>
                    <% } else { %>
                        <li class="nav-item"><a href="/login" class="btn btn-nav btn-login">Đăng nhập</a></li>
                        <li class="nav-item"><a href="/register" class="btn btn-nav btn-register">Đăng ký ngay</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <section class="hero-section">
        <div class="container">
            <div class="hero-badge"><i class="fas fa-bolt"></i> Kết nối chỉ trong 30 giây</div>
            <h1 class="hero-title">Tìm kiếm tri thức,<br>Kiến tạo tương lai.</h1>
            <p class="hero-subtitle">Nền tảng kết nối Giáo viên và Học viên theo mô hình kinh tế chia sẻ. Thanh toán bằng Token linh hoạt, không ràng buộc.</p>

            <form action="/learner" method="GET" class="search-wrapper">
                <i class="fas fa-search search-icon"></i>
                <input type="text" name="search" class="search-input" placeholder="Bạn muốn học kỹ năng gì? (VD: Python, IELTS, Marketing...)">
                <button type="submit" class="btn-search"><i class="fas fa-arrow-right"></i></button>
            </form>

            <div class="role-switch-container">
                <a href="/learner" class="role-card">
                    <div class="role-icon-box bg-student"><i class="fas fa-user-graduate"></i></div>
                    <div class="text-start">
                        <div class="fw-bold">Dành cho Học Viên</div>
                        <small class="text-muted">Tìm lớp & Học ngay</small>
                    </div>
                </a>
                <a href="/teacher" class="role-card">
                    <div class="role-icon-box bg-teacher"><i class="fas fa-chalkboard-teacher"></i></div>
                    <div class="text-start">
                        <div class="fw-bold">Dành cho Giáo Viên</div>
                        <small class="text-muted">Đăng ký dạy & Kiếm Token</small>
                    </div>
                </a>
            </div>
        </div>
    </section>

    <section class="container py-5">
        <div class="section-header">
            <h2 class="fw-bold">🔥 Khóa học mới nhất</h2>
            <p class="text-muted">Khám phá các khóa học được đăng bởi cộng đồng EduLink</p>
        </div>

        <div class="row g-4">
            <% if (typeof courses !== 'undefined' && courses.length > 0) { %>
                <% courses.forEach(course => { %>
                    <div class="col-lg-3 col-md-6">
                        <div class="course-card h-100">
                            <img src="<%= course.image_url %>" class="course-thumb-img" alt="Course Image">
                            
                            <div class="course-body">
                                <div class="d-flex justify-content-between mb-2">
                                    <span class="badge bg-light text-dark border">New</span>
                                    <span class="text-warning fw-bold"><i class="fas fa-star"></i> 5.0</span>
                                </div>
                                <h5 class="fw-bold mb-2 text-truncate" title="<%= course.title %>"><%= course.title %></h5>
                                <p class="text-muted small mb-3 text-truncate">GV: <%= course.teacher_name %></p>
                                
                                <div class="d-flex justify-content-between align-items-center mt-3">
                                    <span class="badge-token"><i class="fas fa-coins"></i> <%= course.price_tokens %> Token</span>
                                    
                                    <form action="/enroll" method="POST">
                                        <input type="hidden" name="courseId" value="<%= course.id %>">
                                        <button type="submit" class="btn btn-sm btn-outline-primary rounded-pill fw-bold">
                                            <i class="fas fa-plus"></i> Đăng ký
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12 text-center py-5">
                    <div class="opacity-50 mb-3"><i class="fas fa-box-open fa-4x"></i></div>
                    <h4>Chưa có khóa học nào được đăng!</h4>
                    <p class="text-muted">Hãy đăng nhập tài khoản Giáo viên và đăng khóa học đầu tiên nhé.</p>
                </div>
            <% } %>
        </div>
    </section>

    <div class="container">
        <section class="stat-section">
            <div class="row text-center">
                <div class="col-md-4 stat-item">
                    <h2>15,000+</h2>
                    <p class="opacity-75">Học viên hoạt động</p>
                </div>
                <div class="col-md-4 stat-item border-start border-end border-secondary">
                    <h2>2,500+</h2>
                    <p class="opacity-75">Giáo viên Verified</p>
                </div>
                <div class="col-md-4 stat-item">
                    <h2>98%</h2>
                    <p class="opacity-75">Đánh giá hài lòng</p>
                </div>
            </div>
        </section>
    </div>

    <footer class="footer text-center">
        <div class="container">
            <h4 class="fw-bold text-primary mb-3">EduLink AI</h4>
            <p class="text-muted mb-4">Dự án khởi nghiệp INNOGREENLIFE 2026. Kết nối tri thức - Kiến tạo tương lai.</p>
            <div class="d-flex justify-content-center gap-3 mb-4">
                <a href="#" class="btn btn-light rounded-circle"><i class="fab fa-facebook-f"></i></a>
                <a href="#" class="btn btn-light rounded-circle"><i class="fab fa-tiktok"></i></a>
                <a href="#" class="btn btn-light rounded-circle"><i class="fab fa-youtube"></i></a>
            </div>
            <small class="text-muted">&copy; 2026 EduLink Team. All rights reserved.</small>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
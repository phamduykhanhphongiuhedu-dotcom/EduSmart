<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Đăng nhập - EduLink AI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: #fff; height: 100vh; overflow: hidden; }
        .split-screen { display: flex; height: 100%; }
        
        /* Cột trái: Ảnh nghệ thuật */
        .left-pane {
            flex: 1;
            background: url('https://images.unsplash.com/photo-1522202176988-66273c2fd55f?q=80&w=2071&auto=format&fit=crop') no-repeat center center/cover;
            position: relative;
            display: none; /* Ẩn trên mobile */
        }
        .left-pane::after {
            content: ''; position: absolute; top:0; left:0; width:100%; height:100%;
            background: linear-gradient(to bottom, rgba(79, 70, 229, 0.4), rgba(15, 23, 42, 0.8));
        }
        .caption {
            position: absolute; bottom: 50px; left: 50px; z-index: 2; color: white; max-width: 80%;
        }
        
        /* Cột phải: Form */
        .right-pane {
            flex: 1; display: flex; align-items: center; justify-content: center; padding: 40px; background: white;
        }
        .login-box { width: 100%; max-width: 400px; }
        .form-control {
            padding: 15px 20px; border: 1px solid #e2e8f0; background: #f8fafc; font-weight: 500;
        }
        .form-control:focus { border-color: #4f46e5; box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1); background: white; }
        
        /* Input Group Customization */
        .input-group-text { border-color: #e2e8f0; }
        
        .btn-login {
            background: #4f46e5; color: white; border: none; padding: 15px; border-radius: 12px; font-weight: 700; width: 100%; transition: 0.3s;
        }
        .btn-login:hover { background: #4338ca; transform: translateY(-2px); box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3); }
        
        @media (min-width: 992px) { .left-pane { display: block; } }
    </style>
</head>
<body>
    <div class="split-screen">
        <div class="left-pane">
            <div class="caption">
                <h1 class="fw-bold display-5">Học tập không giới hạn.</h1>
                <p class="lead opacity-75">Tham gia cộng đồng học tập thông minh số 1 Việt Nam ngay hôm nay.</p>
            </div>
        </div>
        <div class="right-pane">
            <div class="login-box">
                <div class="mb-5">
                    <h2 class="fw-bold mb-2">Chào mừng trở lại! 👋</h2>
                    <p class="text-muted">Vui lòng nhập thông tin để đăng nhập.</p>
                </div>
                
                <form action="/login" method="POST">
                    <div class="mb-3">
                        <label class="fw-bold small text-muted mb-2">Email tài khoản đăng nhập</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="fas fa-user text-muted"></i></span>
                            <input type="text" name="username" class="form-control border-start-0" placeholder="Ví dụ: hocsinh1" required>
                        </div>
                    </div>
                    
                    <div class="mb-4">
                        <label class="fw-bold small text-muted mb-2">Mật khẩu</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="fas fa-lock text-muted"></i></span>
                            
                            <input type="password" name="password" id="loginPassInput" class="form-control border-start-0 border-end-0" placeholder="••••••" required>
                            
                            <span class="input-group-text bg-white border-start-0" style="cursor: pointer;" onclick="toggleLoginPass()">
                                <i class="fas fa-eye text-muted" id="loginPassIcon"></i>
                            </span>
                        </div>
                    </div>

                    <button type="submit" class="btn-login mb-4">Đăng Nhập</button>
                    
                    <div class="text-center">
                        <small class="text-muted">Chưa có tài khoản? <a href="/register" class="fw-bold text-primary text-decoration-none">Đăng ký ngay</a></small>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        function toggleLoginPass() {
            const passInput = document.getElementById('loginPassInput');
            const passIcon = document.getElementById('loginPassIcon');

            if (passInput.type === 'password') {
                passInput.type = 'text'; // Hiện
                passIcon.classList.remove('fa-eye');
                passIcon.classList.add('fa-eye-slash'); // Icon mắt gạch chéo
            } else {
                passInput.type = 'password'; // Ẩn
                passIcon.classList.remove('fa-eye-slash');
                passIcon.classList.add('fa-eye'); // Icon mắt thường
            }
        }
    </script>
</body>
</html>
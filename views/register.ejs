<!DOCTYPE html>
<html lang="vi">
<head>
    <title>Đăng ký EduSmart Account</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background: #f1f5f9; display: flex; align-items: center; justify-content: center; height: 100vh; font-family: sans-serif; }
        .card-reg { background: white; padding: 40px; border-radius: 20px; box-shadow: 0 10px 40px rgba(0,0,0,0.08); width: 100%; max-width: 500px; }
        .btn-google { background: white; border: 1px solid #cbd5e1; color: #334155; font-weight: 600; width: 100%; padding: 12px; border-radius: 10px; display: flex; align-items: center; justify-content: center; gap: 10px; transition: 0.2s; }
        .btn-google:hover { background: #f8fafc; border-color: #94a3b8; }
    </style>
</head>
<body>
    <div class="card-reg">
        <h3 class="fw-bold text-center mb-4" style="color: #4f46e5;">EduSmart ID</h3>
        
        <button class="btn-google mb-3" onclick="alert('Tính năng đang phát triển!')">
            <img src="https://upload.wikimedia.org/wikipedia/commons/5/53/Google_%22G%22_Logo.svg" width="20">
            Đăng ký bằng Google
        </button>
        
        <div class="text-center text-muted mb-3 small fw-bold">- HOẶC TẠO TÀI KHOẢN EDUSMART -</div>

        <form action="/register" method="POST">
            <div class="mb-3">
                <label class="fw-bold small mb-2 text-muted">Họ và Tên</label>
                <input type="text" name="fullname" class="form-control p-3 rounded-3" placeholder="VD: Trần Văn B" required>
            </div>

            <div class="mb-3">
                <label class="fw-bold small mb-2 text-muted">Tự đặt mật khẩu</label>
                
                <div class="input-group">
                    <input type="password" name="password" id="passwordInput" 
                        class="form-control p-3 border-end-0" 
                        placeholder="VD: MatKhau@123" required 
                        title="Mật khẩu phải có ít nhất 8 ký tự, bao gồm chữ hoa, số và ký tự đặc biệt.">
                    
                    <span class="input-group-text bg-white border-start-0" style="cursor: pointer;" onclick="togglePassword()">
                        <i class="fas fa-eye text-muted" id="toggleIcon"></i>
                    </span>
                </div>

                <div class="form-text small text-muted">
                    <i class="fas fa-info-circle"></i> Yêu cầu: 8+ ký tự, Chữ Hoa, Số & Ký tự đặc biệt (VD: @, #).
                </div>
                <div class="form-text text-danger small mt-1">
                    * Không được trùng với Mã ID hoặc Email.
                </div>
            </div>
            
            <div class="mb-4">
                <label class="fw-bold small mb-2 text-muted">Bạn là ai?</label>
                <div class="d-flex gap-3">
                    <label class="btn btn-outline-primary flex-fill rounded-3 p-3 border-2 fw-bold">
                        <input type="radio" name="role" value="learner" checked> 🎓 Học viên
                    </label>
                    <label class="btn btn-outline-warning flex-fill rounded-3 p-3 border-2 fw-bold">
                        <input type="radio" name="role" value="teacher"> 👩‍🏫 Giáo viên
                    </label>
                </div>
            </div>

            <button type="submit" class="btn w-100 p-3 rounded-3 fw-bold text-white shadow-sm" style="background: #4f46e5;">Cấp Tài Khoản Mới</button>
        </form>
        <div class="text-center mt-3">
            <a href="/login" class="text-decoration-none">Đã có tài khoản? <b>Đăng nhập</b></a>
        </div>
    </div>
    <script>
    function togglePassword() {
        const passwordInput = document.getElementById('passwordInput');
        const toggleIcon = document.getElementById('toggleIcon');

        if (passwordInput.type === 'password') {
            // Hiện mật khẩu
            passwordInput.type = 'text';
            toggleIcon.classList.remove('fa-eye');
            toggleIcon.classList.add('fa-eye-slash'); // Icon mắt gạch chéo
        } else {
            // Ẩn mật khẩu
            passwordInput.type = 'password';
            toggleIcon.classList.remove('fa-eye-slash');
            toggleIcon.classList.add('fa-eye'); // Icon mắt mở
        }
    }
</script>
</body>
</html>